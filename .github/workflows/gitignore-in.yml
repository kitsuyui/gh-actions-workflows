name: Reusable gitignore-in update

on:
  workflow_call:

permissions:
  contents: write
  pull-requests: write

jobs:
  update-gitignore:
    runs-on: ubuntu-latest
    steps:
      - uses: gitignore-in/gh-action@v0.2.3
